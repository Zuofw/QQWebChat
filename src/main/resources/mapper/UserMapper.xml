<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bronya.qqchat.mapper.UserMapper">


    <select id="getMessageById" resultType="com.bronya.qqchat.domain.entity.Message">
        select * from message where from_user_id = #{fromUserId} and to_user_id = #{toUserId}
        order by create_time desc

    </select>
</mapper>
